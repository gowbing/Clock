package com.miracle.clock.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.miracle.clock.model.normal.DateRemind;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DATE_REMIND".
*/
public class DateRemindDao extends AbstractDao<DateRemind, Long> {

    public static final String TABLENAME = "DATE_REMIND";

    /**
     * Properties of entity DateRemind.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property Time = new Property(1, long.class, "time", false, "TIME");
        public final static Property Repeat_1 = new Property(2, long.class, "repeat_1", false, "REPEAT_1");
        public final static Property Repeat_2 = new Property(3, long.class, "repeat_2", false, "REPEAT_2");
        public final static Property Repeat_3 = new Property(4, long.class, "repeat_3", false, "REPEAT_3");
        public final static Property Content = new Property(5, String.class, "content", false, "CONTENT");
        public final static Property Isstart = new Property(6, boolean.class, "isstart", false, "ISSTART");
    };


    public DateRemindDao(DaoConfig config) {
        super(config);
    }
    
    public DateRemindDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DATE_REMIND\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: id
                "\"TIME\" INTEGER NOT NULL ," + // 1: time
                "\"REPEAT_1\" INTEGER NOT NULL ," + // 2: repeat_1
                "\"REPEAT_2\" INTEGER NOT NULL ," + // 3: repeat_2
                "\"REPEAT_3\" INTEGER NOT NULL ," + // 4: repeat_3
                "\"CONTENT\" TEXT," + // 5: content
                "\"ISSTART\" INTEGER NOT NULL );"); // 6: isstart
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DATE_REMIND\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DateRemind entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindLong(2, entity.getTime());
        stmt.bindLong(3, entity.getRepeat_1());
        stmt.bindLong(4, entity.getRepeat_2());
        stmt.bindLong(5, entity.getRepeat_3());
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(6, content);
        }
        stmt.bindLong(7, entity.getIsstart() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DateRemind entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
        stmt.bindLong(2, entity.getTime());
        stmt.bindLong(3, entity.getRepeat_1());
        stmt.bindLong(4, entity.getRepeat_2());
        stmt.bindLong(5, entity.getRepeat_3());
 
        String content = entity.getContent();
        if (content != null) {
            stmt.bindString(6, content);
        }
        stmt.bindLong(7, entity.getIsstart() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public DateRemind readEntity(Cursor cursor, int offset) {
        DateRemind entity = new DateRemind( //
            cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // time
            cursor.getLong(offset + 2), // repeat_1
            cursor.getLong(offset + 3), // repeat_2
            cursor.getLong(offset + 4), // repeat_3
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // content
            cursor.getShort(offset + 6) != 0 // isstart
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DateRemind entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setTime(cursor.getLong(offset + 1));
        entity.setRepeat_1(cursor.getLong(offset + 2));
        entity.setRepeat_2(cursor.getLong(offset + 3));
        entity.setRepeat_3(cursor.getLong(offset + 4));
        entity.setContent(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setIsstart(cursor.getShort(offset + 6) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DateRemind entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DateRemind entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
